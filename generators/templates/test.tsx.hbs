import { render, screen } from '@testing-library/react'

import { {{pascalCase name}} } from '.'

describe('render {{pascalCase name}}', () => {
  it('should render a {{pascalCase name}}', () => {
    render(<{{pascalCase name}} />)
  })
})
