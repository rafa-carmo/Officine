import { render, screen } from '@testing-library/react'

import { DashboardTableBudget } from '.'

describe('render DashboardTableBudget', () => {
  it('should render a DashboardTableBudget', () => {
    render(<DashboardTableBudget />)
  })
})
