import { render, screen } from '@testing-library/react'

import { SliderNews } from '.'

describe('render SliderNews', () => {
  it('should render a SliderNews', () => {
    render(<SliderNews />)
  })
})
